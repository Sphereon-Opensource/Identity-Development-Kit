# Module idk-bom

IDK Bill of Materials (BOM). Declares aligned versions for every published IDK module so downstream consumers can import a single platform dependency instead of pinning every artifact individually. Contains no runtime code.
