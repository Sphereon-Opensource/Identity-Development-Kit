# Module lib-mdoc-transport-ble

Aggregator that bundles the `-public` and `-impl` halves of the mdoc BLE transport. Reach for it from consumer modules that want the full BLE transport in one artifact; depend on the split modules directly if you have to separate contract from implementation.
