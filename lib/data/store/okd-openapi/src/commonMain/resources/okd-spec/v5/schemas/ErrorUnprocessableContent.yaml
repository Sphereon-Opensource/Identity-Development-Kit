description: Unprocessable Content
content:
  application/problem+json:
    schema:
      $ref: 'Problem.yaml'
    example:
      type: 'https://open-education-api.github.io/specification/v5/schemas/ErrorUnprocessableContent.yaml'
      title: 'Unprocessable Content'
      status: 422
      detail: 'the server understood the content type of the request content, and the syntax of the request content was correct, but it was unable to process the contained instructions.'